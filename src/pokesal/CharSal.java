package pokesal;

/**
 * Classe que representa a especie de Pokesal CharSal.java.
 */
public class CharSal extends Pokesal {
    public CharSal(String nome, int hp, int atk, int def, int spd, TipoElemental tipo) {
        super(nome, hp, atk, def, spd, tipo);
    }
}
