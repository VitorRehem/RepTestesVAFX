package pokesal;

/**
 * Classe que representa a especie de Pokesal SquirSal.java.
 */
public class SquirSal extends Pokesal {
    public SquirSal(String nome, int hp, int atk, int def, int spd, TipoElemental tipo) {
        super(nome, hp, atk, def, spd, tipo);
    }
}
