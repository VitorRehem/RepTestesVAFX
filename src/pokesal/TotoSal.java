package pokesal;

/**
 * Classe que representa a especie de Pokesal TotoSal.java.
 */
public class TotoSal extends Pokesal {
    public TotoSal(String nome, int hp, int atk, int def, int spd, TipoElemental tipo) {
        super(nome, hp, atk, def, spd, tipo);
    }
}
